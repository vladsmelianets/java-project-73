server:
  port: ${PORT:5000}

base-url: /api

spring:
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yml

  jpa:
    hibernate:
      ddl-auto: create-drop