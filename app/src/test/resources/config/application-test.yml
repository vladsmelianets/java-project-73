#base-url: /api

spring:
  datasource:
    username: ""
    password: ""
    url: jdbc:h2:mem:db

  liquibase:
    enabled: true
    change-log: classpath:db/changelog/changelog-master.yml

logging:
  level:
    root: info